# Simple Web Server

## Description
This project implements a basic HTTP web server using Python’s built-in libraries. 
It handles HTTP GET requests, performs routing, and returns different types of content
including JSON, XML, CSV, and HTML.

## Features
- Custom HTTP server
- Routing using request paths
- JSON, XML, CSV responses
- HTML served as string and file
- No external frameworks

## Run Instructions
```bash
python server.py
